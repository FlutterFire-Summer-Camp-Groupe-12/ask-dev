tags:
  # Captures d'écran de référence : dépendantes du moteur de rendu de la
  # machine. Les regénérer avec `flutter test --update-goldens` après un
  # changement de design, ou les exclure avec `--exclude-tags golden`.
  golden:
